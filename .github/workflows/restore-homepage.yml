name: Homepage restore safeguard

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  restore:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: homepage upgrades must not be overwritten by an automatic rollback."
